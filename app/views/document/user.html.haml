%h1= t('documents.user.title')

= prev_next_pagination(@documents)

%ul
  - @documents.each do |doc|
    %li
      %h5
        = link_to doc.title, doc
        \|
        %a.text-warning{href: edit_document_path(doc)}
          %i.glyphicon.glyphicon-edit